import type { AdminViewServerProps } from 'payload'
import CustomDashboard from '@/components/admin/CustomDashboard'

export default function CustomDashboardView(_props: AdminViewServerProps) {
  return <CustomDashboard />
}
